apiVersion: v1
kind: Service
metadata:
  name: {{ include "multiagent-social.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "multiagent-social.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

